
{% extends 'pages/baseInGame.html.twig' %}

{% block body %}

    <div class="container">
        <h1 class = "jumbotron-heading"> Voici les donjons disponibles !</h1>
        <div class="div row flex">
            {% for dungeon in dungeons %}
                <div class="col-3">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">
                                <a> {{ dungeon.name }}</a>
                            </h5>
                        </div>
                        <a href="{{ path('editDungeon', {'id': dungeon.id}) }}" class="btn btn-primary">Modifier</a><br>
                        <a href="{{ path('deleteDungeon', {'id': dungeon.id}) }}" class="btn btn-danger">Supprimer</a>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>


{% endblock %}